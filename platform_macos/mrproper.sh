#!/usr/bin/env bash

# removes all the source files and the executables

rm -rv bin/
rm -rv lib/
rm -rv share/
rm -rv src/dfu-util-0.*
rm -rv src/libusb-1.0.*
rm -rv src/make-*
rm -rv src/dfu-util-0.*.tar.gz
rm -rv src/libusb-1.0.*.tar.bz2
rm -rv src/make-*.tar.gz
rm -rv ../chibios
